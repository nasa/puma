#ifndef PUMA_GENERATE_H
#define PUMA_GENERATE_H

#include "operation.h"


//! A base class from which the classes for generating geometries
class Generate : Operation
{
public:
    //static unknownReturn computeProperty(ImportInput in);
    Generate() = default;

};

#endif // PUMA_GENERATE_H

